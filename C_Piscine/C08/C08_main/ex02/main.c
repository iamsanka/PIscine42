#include <stdio.h>
#include "ft_abs.h"

int main() {
    int a = -5;
    int b = 3;
    int c = 0;

    printf("ABS of %d is %d\n", a, ABS(a)); 
    printf("ABS of %d is %d\n", b, ABS(b)); 
    printf("ABS of %d is %d\n", c, ABS(c)); 

    return 0;
}
